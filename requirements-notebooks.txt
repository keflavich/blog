# Optional tools for authoring posts from Jupyter notebooks (see nbconverter.py
# and rst.tpl). These are NOT needed to build or deploy the site -- content is
# committed as .rst -- so they are kept out of requirements.txt to keep CI lean.
# Install locally with: pip install -r requirements-notebooks.txt
graphviz
jupyter
jupyter-client
jupyter-console
jupyter-core
nbconvert
nbformat
notebook
pandocfilters
